[project]
name = "pipeline_runner"
version = "0.1.0"
description = "End-to-End Pipeline Execution System"
requires-python = ">=3.10"

dependencies = []

[tool.setuptools.packages.find]
include = ["pipeline_runner*"]